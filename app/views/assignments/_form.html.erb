<%= form_for(@assignment) do |f| %>
  <% if @assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% @assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Arbeitsstunden auswählen' %><br />
    <%= f.collection_select :calculation_id, Calculation.all, :id, :workhours_perDay %>
  </div>

  <div class="actions">
    <%= f.submit "Operationsplan anlegen oder aktualisieren" %>
  </div>
<% end %>
